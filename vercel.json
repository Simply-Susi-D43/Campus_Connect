{
  "framework": "vite",
  "buildCommand": "npx tsc -b && npx vite build",
  "outputDirectory": "dist",
  "functions": {},
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
